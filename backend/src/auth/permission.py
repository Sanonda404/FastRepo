# rule:
# if user is owner then can push anywhere
# if collaborator then also can push anywhere, if not added to any team
# if collaborator becomes part of team then can only push to specified branch/folder